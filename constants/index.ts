/**
 * App-wide constants for values reused across multiple modules.
 */

/**
 * Outlined pill button used for secondary actions (download, retry, back, …).
 * Shared so the resume, error, and project-detail pages stay visually in sync.
 */
export const ACTION_BUTTON_CLASS =
  "inline-flex items-center gap-1.5 rounded-lg border border-border bg-background px-3 py-1.5 text-sm text-foreground transition-colors hover:bg-muted";
